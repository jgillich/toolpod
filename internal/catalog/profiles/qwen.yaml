version: 1
meta:
  description: Qwen Code CLI (Alibaba)
extends:
  - defaults
  - mise
command: ["qwen"]
tools:
  qwen: latest
mounts:
  ~/.qwen:
    source: ~/.qwen
    create: true
    read_only: false
