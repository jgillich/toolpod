version: 1
extends: mise
command: ["claude"]
tools:
  claude-code: latest
mounts:
  ~/.claude:
    source: ~/.claude
    create: true
    read_only: false
  ~/.cache/claude-code:
    source: ~/.cache/claude-code
    create: true
    read_only: false
