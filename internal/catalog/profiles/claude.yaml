version: 1
extends: mise
command: ["claude"]
tools:
  claude-code: latest
mounts:
  ~/.claude:
    source: ~/.claude
    read_only: false
  ~/.cache/claude-code:
    source: ~/.cache/claude-code
    read_only: false
