version: 1
meta:
  description: OpenAI Codex CLI
extends:
  - defaults
  - mise
command: ["codex"]
tools:
  codex: latest
mounts:
  ~/.codex:
    source: ~/.codex
    create: true
    read_only: false
