version: 1
extends: mise
command: ["trivy"]
caches:
  trivy: ~/.cache/trivy
mounts:
  ~/.config/trivy:
    source: ~/.config/trivy
    create: true
    read_only: false
    optional: true
tools:
  trivy: latest
