version: 1
meta:
  description: Pi, the minimal terminal coding agent
extends:
  - defaults
  - mise
command: ["pi"]
tools:
  pi: latest
mounts:
  ~/.pi:
    source: ~/.pi
    create: true
    read_only: false
