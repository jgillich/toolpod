version: 1
meta:
  description: GitLab CLI (glab)
extends: vcs/git
mounts:
  ~/.config/glab-cli:
    source: ~/.config/glab-cli
    create: true
    read_only: false
    optional: true
tools:
  glab: latest
